# Mission 02: 과일 자르기 (Fruit Slice)

## 게임 정보
- **ID**: 2
- **이름**: 과일 자르기
- **카테고리**: 액션/스와이프
- **난이도**: ⭐ (쉬움)

## 게임 설명
화면을 가로지르는 과일을 스와이프로 자르는 게임입니다.

## 조작 방법
- **입력**: 스와이프 👉
- **액션**: 과일 자르기

## 시작 전 지시문
```
스와이프! 👉
```

## 게임 규칙
1. 과일이 화면 아래에서 위로 날아옴
2. 스와이프로 과일을 자름
3. 폭탄은 자르면 안됨 (실패)
4. 제한시간 내에 목표 점수 달성

## 점수 시스템
- **타입**: 정확도 기반
- **점수 계산**: `(자른 과일 / 전체 과일) × 100`
- **기본 목표 점수**: 70점 (70% 성공률)

### 난이도별 목표 점수
| 난이도 | 목표 점수 | 과일 수 | 폭탄 수 |
|--------|----------|---------|---------|
| Lv.1 | 70점 | 10개 | 2개 |
| Lv.2 | 75점 | 12개 | 3개 |
| Lv.3 | 80점 | 15개 | 4개 |
| Lv.4 | 85점 | 18개 | 5개 |
| Lv.5 | 90점 | 20개 | 6개 |
| Lv.6 | 95점 | 25개 | 7개 |

## 제한 시간
- **기본**: 12초
- **난이도 조정**: 난이도별 동일

## 난이도별 변화
| 난이도 | 발사 속도 | 과일 크기 | 동시 등장 |
|--------|----------|----------|----------|
| Lv.1 | 느림 | 큼 (60px) | 1개 |
| Lv.2 | 느림 | 보통 (55px) | 1-2개 |
| Lv.3 | 보통 | 보통 (50px) | 2개 |
| Lv.4 | 빠름 | 작음 (45px) | 2-3개 |
| Lv.5 | 빠름 | 작음 (40px) | 3개 |
| Lv.6 | 매우 빠름 | 매우 작음 (35px) | 3-4개 |

## 하드 모드
- 과일이 회전하면서 날아옴
- 폭탄 수 2배 증가

## 비주얼 구현

### 과일/폭탄
```
과일 이모지: 🍎🍊🍋🍇🍓🥝
폭탄 이모지: 💣
자른 효과: 과일이 반으로 쪼개짐 + 주스 파티클
폭탄 효과: 💥 + 화면 흔들림
```

### 스와이프 궤적
```javascript
// 스와이프 라인 표시
ctx.strokeStyle = '#FFD700';
ctx.lineWidth = 3;
ctx.lineCap = 'round';
// 사용자가 스와이프한 경로를 짧게 표시
```

## 구현 로직

```typescript
interface FruitObject {
  id: number;
  x: number;
  y: number;
  vx: number;         // X 속도
  vy: number;         // Y 속도
  type: 'fruit' | 'bomb';
  emoji: string;
  size: number;
  rotation: number;   // 회전 각도
  rotationSpeed: number;
  sliced: boolean;
}

function handleSwipe(startX: number, startY: number, endX: number, endY: number) {
  objects.forEach(obj => {
    if (obj.sliced) return;
    
    // 스와이프 라인과 과일의 충돌 감지
    if (lineIntersectsCircle(startX, startY, endX, endY, obj.x, obj.y, obj.size)) {
      if (obj.type === 'fruit') {
        sliceFruit(obj);
        score += 10;
      } else {
        hitBomb();
        // 게임 즉시 실패
      }
    }
  });
}
```

## 사운드 효과
- 과일 자를 때: "샤악!" 효과음
- 폭탄 터질 때: "쾅!" 효과음

## 진동 효과
- 과일 자를 때: 짧은 진동 (30ms)
- 폭탄 터질 때: 강한 진동 (300ms)
