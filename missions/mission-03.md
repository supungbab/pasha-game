# Mission 03: 두더지 잡기 (Whack-a-Mole)

## 게임 정보
- **ID**: 3
- **이름**: 두더지 잡기
- **카테고리**: 반사신경/타이밍
- **난이도**: ⭐ (쉬움)

## 게임 설명
9개의 구멍에서 나타나는 두더지를 빠르게 탭하여 잡는 게임입니다.

## 조작 방법
- **입력**: 탭 👆
- **액션**: 두더지 때리기

## 시작 전 지시문
```
탭! 👆
```

## 게임 규칙
1. 3x3 격자로 배치된 9개의 구멍
2. 두더지가 랜덤하게 나타남
3. 나타난 두더지를 빠르게 탭
4. 일정 시간 후 두더지는 숨음

## 점수 시스템
- **타입**: 성공 횟수 기반
- **점수 계산**: `잡은 두더지 수 × 5점`
- **기본 목표 점수**: 50점 (10마리)

### 난이도별 목표 점수
| 난이도 | 목표 점수 | 필요 두더지 |
|--------|----------|-------------|
| Lv.1 | 50점 | 10마리 |
| Lv.2 | 60점 | 12마리 |
| Lv.3 | 75점 | 15마리 |
| Lv.4 | 90점 | 18마리 |
| Lv.5 | 110점 | 22마리 |
| Lv.6 | 125점 | 25마리 |

## 제한 시간
- **기본**: 15초

## 난이도별 변화
| 난이도 | 노출 시간 | 생성 간격 | 동시 출현 |
|--------|----------|----------|----------|
| Lv.1 | 1.5초 | 1.5초 | 1-2마리 |
| Lv.2 | 1.3초 | 1.3초 | 1-2마리 |
| Lv.3 | 1.0초 | 1.0초 | 2-3마리 |
| Lv.4 | 0.8초 | 0.8초 | 2-3마리 |
| Lv.5 | 0.6초 | 0.6초 | 3-4마리 |
| Lv.6 | 0.5초 | 0.5초 | 3-4마리 |

## 하드 모드
- 가짜 두더지 등장 (빨간색) - 잡으면 점수 차감
- 두더지가 더 빠르게 숨음

## 비주얼 구현
```
구멍: ⚫ (검은 원)
두더지: 🐹 (위로 올라옴 애니메이션)
가짜 두더지: 😈
잡았을 때: ⭐ 효과
```

## 구현 로직
```typescript
interface Hole {
  id: number;
  x: number;
  y: number;
  mole: Mole | null;
}

interface Mole {
  type: 'normal' | 'fake';
  showTime: number;
  progress: number;  // 0-1, 올라오는 진행도
}

function spawnMole() {
  const emptyHoles = holes.filter(h => !h.mole);
  const hole = randomChoice(emptyHoles);
  
  hole.mole = {
    type: isHardMode && Math.random() < 0.2 ? 'fake' : 'normal',
    showTime: getMoleShowTime(difficulty),
    progress: 0
  };
}

function handleTap(x: number, y: number) {
  const tappedHole = holes.find(h => {
    return h.mole && distance(x, y, h.x, h.y) < holeRadius;
  });
  
  if (tappedHole?.mole) {
    if (tappedHole.mole.type === 'normal') {
      score += 5;
      playSound('hit');
    } else {
      score -= 10;
      playSound('wrong');
    }
    tappedHole.mole = null;
  }
}
```